from distutils.core import setup

setup(name='crypto_exchange',
      version='1.0',
      description='Cryptocurrency exchange',
      packages=['crypto_exchange'],
      install_requires=[
        "pandas",
      ]
     )